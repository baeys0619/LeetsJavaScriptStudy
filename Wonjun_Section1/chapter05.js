//1. Number Type
let num1=27;
let num2=1.5;
let num3=-20;

let inf=Infinity;
let mInf=-Infinity;

let nan=NaN; //Not a Number

//2. String Type
let myName="이정환";
let myLocation="목동";
let introduce= myName +myLocation;

let introduceText= `${myName}은  ${myLocation}에 삽니다.`;
//템플릿 리터럴 문법

//3. Boolean Type

let isSwitchOn=true;
let isEmpty=false;

//4. Null Type(아무것도 없다)l
let empty=null;

//5. Undefined Type(값이 할당되지 않음)
let none;
console.loglog(none); //undefined 출력
let undef2; //자동으로 undefined 할당됨
